const PAGE = `<!doctype html>
<html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="referrer" content="no-referrer"><title>Pair Pomegr</title>
<style>:root{color-scheme:light dark;--canvas:#f3f4f5;--panel:#ffffff;--ink:#20252b;--muted:#525b66;--line:#d4d9df;--focus:#255fa1}@media(prefers-color-scheme:dark){:root{--canvas:#111315;--panel:#191c20;--ink:#edf0f3;--muted:#a8afb9;--line:#323840;--focus:#91b8ee}}body{margin:0;background:var(--canvas);color:var(--ink);font:14px/1.5 Inter,system-ui,sans-serif;display:grid;min-height:100vh;min-height:100dvh;place-items:center}.card{box-sizing:border-box;width:min(100% - 32px,480px);padding:32px;border:1px solid var(--line);border-radius:6px;background:var(--panel)}h1{margin:0 0 12px;font-size:26px;line-height:1.2;letter-spacing:-.02em}p{color:var(--muted);margin:0}button{margin-top:24px;min-height:44px;border:0;border-radius:4px;padding:0 18px;background:#a63c32;color:#ffffff;font:600 14px Inter,system-ui,sans-serif;cursor:pointer}button:hover{filter:brightness(1.1)}button:focus-visible{outline:2px solid var(--focus);outline-offset:3px}button[hidden]{display:none}::selection{background:#a63c32;color:#ffffff}</style>
<main class="card"><h1>Pair Pomegr</h1><p id="status" role="status" aria-live="polite">Connecting this browser…</p><button id="retry" hidden>Try again</button></main>
<script>(function(){const status=document.getElementById('status'),retry=document.getElementById('retry');let pending=false;function recovery(){status.textContent='This pairing code could not be used. Generate a new code on the computer.';retry.hidden=true}async function pair(){if(pending)return;const secret=location.hash.slice(1);if(!secret){recovery();return}pending=true;retry.hidden=true;try{const r=await fetch('/__pomegr/pair',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({secret})});if(r.status===429){status.textContent='Pomegr cannot accept another pairing attempt right now. Manage sharing or create a new code on the computer.';return}if(!r.ok){recovery();return}history.replaceState(null,'','/__pomegr/pair');location.replace('/')}catch(_){status.textContent='Pomegr could not be reached. Check the connection and try again.';retry.hidden=false}finally{pending=false}}retry.addEventListener('click',pair);pair()})()</script>`;

export function lanPairingPage() {
  return PAGE;
}
